[project]
name = "neuroevolution"
version = "0.1.0"
description = "Prototype tools for plotting NeuroEvolution simulation output."
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.8",
]
